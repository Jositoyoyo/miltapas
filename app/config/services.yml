# http://symfony.com/doc/current/book/service_container.html
parameters:
   enable_AppBanner: true

services:
    service_name:
        class: AppBundle\Directory\AppBanner
        arguments: ["%enable_AppBanner%"]
