{% extends 'base.html.twig' %}

{% block body %}
    <h2>comentar {{local.nombre}} </h2>
     {{ form_start(form) }}

        {% if form_errors(form) %}
            <div class="ui-state-error">{{ form_errors(form) }}</div>
        {% endif %}
        <input type="submit" value="Create" />
        {{ form_end(form) }}


{% endblock %}
{% block scripts %}
    <script>
      
    </script>
{% endblock %}
